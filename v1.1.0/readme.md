This features the exploit, the unlink exploit
- In `./exploit`, there's an example of how it could be exploited to spawn a shell
- In `./tests/`, there's a simple test of the exploit to overwrite a value
